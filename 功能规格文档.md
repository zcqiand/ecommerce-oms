# 电商订单与库存管理系统 — 功能规格文档

> 本文档描述 电商订单与库存管理系统 的所有功能特性、验收标准与测试策略。
> 与 [CLAUDE.md](CLAUDE.md) 共同构成本仓的完整规范文档。

## 功能清单

### 商品管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 商品列表查询 | 分页查询商品，含名称/价格/库存字段 | GET /api/products 返回 200，JSON 数组含 id/name/price/inventoryCount |
| 商品详情查询 | 按 ID 查询单个商品信息 | GET /api/products/{id} 查到返回 200，查不到返回 404 |
| 安全库存预警 | 库存低于阈值时 API 响应含预警标识 | 库存 = 0 时响应中 hasLowStock = true |
| OpenAPI 注解 | 所有 Controller 方法有 @Operation + @ApiResponse | `mvn verify` 通过 springdoc-openapi 验证 |

### 订单管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 订单创建 | 员工提交订单，初始状态 DRAFT | POST /api/orders 返回 201，status = DRAFT |
| 订单状态流转 | DRAFT → SUBMITTED → APPROVED → PAID → SHIPPED → COMPLETED | 各状态转换有且仅有合法的下一状态 |
| 拒绝分支 | 审批被拒时状态变为 REJECTED，附理由 | REJECTED 订单必定有 reason 字段 |
| 取消分支 | 用户可取消 DRAFT 状态订单，状态变为 CANCELLED | DRAFT 订单调用 cancel 返回 200，status = CANCELLED |
| 多级审批 | 金额 < 1000 一级审批，≥ 1000 两级审批（部门经理 + 财务总监） | ≥ 1000 订单 approve 两次才到 PAID，< 1000 一次即到 PAID |

### 库存管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 下单锁库存 | SUBMITTED 时锁定库存（reserved + lockedQuantity） | 订单 SUBMITTED 后库存 reserved_quantity > 0，available_quantity 下降 |
| 支付扣库存 | PAID 时扣减实际库存（locked → sold） | 订单 PAID 后 reserved_quantity 回零，sold_quantity 增加 |
| 取消释放 | CANCELLED / REJECTED / 支付失败时释放锁定的库存 | 上述状态变更后 available_quantity 恢复到下单前水平 |
| 库存查询 | 按商品 ID 查询库存余量 | GET /api/inventory/{productId} 返回 available/reserved/sold 数量 |

### 供应商管理

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 供应商列表 | 查询所有供应商 | GET /api/suppliers 返回 200，含 id/name/contact |
| 供应商关联查询 | 商品可关联供应商 | 商品详情返回 supplierId 与 supplierName |

### 数据库迁移（Flyway）

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 初始 schema | V1__init_schema.sql 创建全部表结构 | 容器启动后 `psql` 查询到 product/order/inventory/supplier/user 表 |
| 种子数据 | V2__seed_data.sql 插入 2 供应商 + 5 商品 + 库存初始数据 | seed 后 product 表有 5 行，supplier 表有 2 行 |
| 禁止 DDL auto | application.yml 中无 ddl-auto: update | 配置审查确认无 hibernate ddl-auto |

### 前端 UI

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 商品列表页 | 展示商品列表，含库存状态 | `npm run build` 成功，页面可加载 |
| 订单列表页 | 展示订单列表，含状态标签 | OrderList.tsx 渲染 status 字段 |
| 订单创建页 | 表单提交创建订单 | SubmitForm.tsx POST 到 /api/orders |

### 容器编排（Docker Compose）

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 三服务启动 | postgres:16-alpine + backend + frontend 一键启动 | `docker compose up -d` 全部服务 running |
| 网络互通 | frontend → backend → postgres 连通 | curl localhost:8080/swagger-ui.html 返回 200 |
| 数据持久化 | postgres 数据卷 survive 重启 | `docker compose restart` 后数据仍在 |

### CI/CD

| 功能名称 | 功能描述 | 验收标准 |
| :--- | :--- | :--- |
| 后端构建 | backend `mvn verify` 通过 | `.github/workflows/ci.yml` backend job 全绿 |
| 前端构建 | frontend `npm run build` 通过 | ci.yml frontend job 全绿 |
| 双 job 并行 | backend 和 frontend 同时触发 | workflow 文件有两个独立 job |

## 测试策略

| 层级 | 目录 | 说明 |
| :--- | :--- | :--- |
| 单元测试 | `backend/src/test/java/` | Controller / Service 层业务逻辑 |
| 集成测试 | `backend/src/test/java/`（SpringBootTest） | 全栈 API 端到端（mock database） |

## 覆盖率阈值

- Lines / Statements / Functions: 80%
- Branches: 75%

## 快速链接

- [README.md](README.md) — 项目介绍与快速开始
- [CLAUDE.md](CLAUDE.md) — 开发约定与编码规范
